# Optional: restrict admin panel to this email (must match a registered user).
# If not set, any logged-in user can access /admin.
# VITE_ADMIN_EMAIL=admin@yourdomain.com

# Supabase (get these from your project: Settings → API)
# When set, videos, rentals, and contact messages are stored in Supabase instead of localStorage.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
